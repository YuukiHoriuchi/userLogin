
// The special key we use to store the interfaces map in each object
var interfaceKey = "$$__interfaces__$$";

// function: _implement
// Make the object 'obj' implement all the listed interfaces
function _implement(obj) {
	map;
	if (!(interfaceKey in obj)) {
		// Give a new map
		obj[interfaceKey] = {};
	}
	for (var i = 1; i < arguments.length; ++i) {
		obj[
	}
	return obj;
}